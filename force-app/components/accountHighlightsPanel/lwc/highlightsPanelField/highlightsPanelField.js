import { LightningElement } from 'lwc';

/**
 * A reusable field component for the highlights panel.
 * Provides consistent styling and structure for label-value pairs.
 * @slot label The label text for the field
 * @slot value The value content for the field
 */
export default class HighlightsPanelField extends LightningElement {}
